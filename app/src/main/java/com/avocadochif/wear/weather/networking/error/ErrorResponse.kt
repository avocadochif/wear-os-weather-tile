package com.avocadochif.wear.weather.networking.error

/**
 * Base top level interface of error responses.
 * All a custom error response models should extend this interface for have ability
 * to be handle custom error handler.
 */
interface ErrorResponse
